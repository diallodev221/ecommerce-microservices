spring:
  application:
    name: discovery-server

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false

  # external resources
  username: eureka
  password: password

server:
  port: 8761

